# 依頼: システムエンジニアの職務経歴書(ペライチ) サイト構築

以下の要件を満たす、シンプルで洗練された1ページ完結 (ペライチ)の職務経歴書Webサイトのコードを出力してください。

## 1. 技術スタック・構成

- **構成:** 単一のHTMLファイル (index.html)

**スタイリング: ** 以下のいずれか(*AIのおすすめ、ただし必要最低限)

選択肢A (AIが最も得意): Tailwind CSS (CDN経由)

難しい場合は最新のCSS CSS Nestingを使用

- **インフラ想定:** Cloudflare Pages (無料枠で静的配信)

##　2.デザイン・思想

**アニメーション: ** 不要 (完全に排除し、人間とAIクローラーの読みやすさを最優先)。

- **レイアウト: ** 上から下へのシングルカラム。

## 3. ページ構成要素

1. プロフィール.md
1. 職歴.md
1. 大事にしているマインド.md

の要素を上から順番に並べていく

## 4.【最重要】AIクローラー・LLM最適化(LLMO)の仕込み

採用AIエージェントやクローラーがパースしやすいよう、以下の2点を必ず実装してください。

1. **セマンティックHTMLの徹底:**

`<div>`を乱用せず、`<article>', '<section>', '<h3>', '<ul>', '<li>`などの意味のあるタグを適切に使い、構造化してください。

技術要素と成果は同じ `<li>` 内に凝縮してください。

2. **JSON-LD (構造化データ)の埋め込み:**

<head>タグ内に、Schema.orgの `ProfilePage`に準拠したJSON-LDをダミーデータ(私のスキルや経歴のメタデータ)で作成し、埋め込んでください。

## 5.出力

すぐにローカルで確認・デプロイできる、完全なコード(HTML/CSS)を1ファイルで出力してください。
